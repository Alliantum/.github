{
    "name": "Automatic release for Odoo's modules",
    "description": "When the `version` key on the `__mainfest__.py` is updated, we compare the latest release and create a new one if necessary.",
    "iconName": "alliantum.svg"
}
